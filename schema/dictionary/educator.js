orion.dictionary.addDefinition('logo', 'educator', orion.attribute('image', {
  label: 'Logo Educator',
}));

orion.dictionary.addDefinition('logoNotHome', 'educator', orion.attribute('image', {
  label: 'Logo not in home',
}));

orion.dictionary.addDefinition('background', 'educator', orion.attribute('image', {
  label: 'Home image',
}));

orion.dictionary.addDefinition('backgroundSmall', 'educator', orion.attribute('image', {
  label: 'Home background for small screens',
}));

orion.dictionary.addDefinition('backgroundMedium', 'educator', orion.attribute('image', {
  label: 'Home background for medium screens',
}));
