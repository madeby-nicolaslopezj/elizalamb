orion.dictionary.addDefinition('logo', 'images', orion.attribute('image', {
  label: 'Logo',
}));

orion.dictionary.addDefinition('background', 'images', orion.attribute('image', {
  label: 'Home background',
}));

orion.dictionary.addDefinition('backgroundSmall', 'images', orion.attribute('image', {
  label: 'Home background for small screens',
}));

orion.dictionary.addDefinition('backgroundMedium', 'images', orion.attribute('image', {
  label: 'Home background for medium screens',
}));
